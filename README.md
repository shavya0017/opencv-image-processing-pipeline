OpenCV Grayscale Converter

A simple computer vision project using OpenCV that converts a color image into grayscale.

Tech Stack

- Python
- OpenCV

Functionality

- Loads an input image
- Converts it to grayscale
- Displays and saves the output image

Usage

Run the script after installing OpenCV:
pip install opencv-python
python main.py


